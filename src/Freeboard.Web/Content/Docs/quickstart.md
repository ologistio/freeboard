# Quickstart

Placeholder page. The steps below show the shapes this template renders; they are not
yet real instructions.

## First heading

Body paragraph placeholder. Write in sentence case, state facts with numbers, and put
the explanation where the confusion happens rather than linking away. Inline `code` and
[links](/docs) both render inside body copy.

### Sub heading

Numbered steps for anything a reader performs in order:

1. Step one placeholder.
2. Step two placeholder.
3. Step three placeholder.

```sh
$ curl https://api.freeboardhq.com/v1/controls \
    -H "Authorization: Bearer $FREEBOARD_API_KEY"
```

> Blockquote placeholder. Rendered as a dashed guidance box - use it for the caveat a
> reader hits two minutes later.

## Second heading

Tables carry reference data that does not belong in prose.

| Field | Type | Notes |
| --- | --- | --- |
| `field_one` | string | Placeholder description of the field. |
| `field_two` | integer | Placeholder description of the field. |
| `field_three` | boolean | Placeholder description of the field. |
