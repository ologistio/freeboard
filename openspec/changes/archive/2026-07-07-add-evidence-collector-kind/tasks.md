## 1. Config kind: parse and model (feat(gitops): core model + loader)

- [x] 1.1 In `src/Freeboard.Core/GitOps/ConfigModel.cs` add `KindEvidenceCollector` to `GitOpsSchema`; add a `sealed record EvidenceCollector` (`ApiVersion, Kind, Id, Title, Control, Vendor, Type, Frequency, Threshold, Config`) where `Config` is a `Dictionary<string, string>` (empty when absent) and `Threshold` is raw authored text (`string Threshold = string.Empty`, matching the raw-string enum fields the validator range-checks) so a malformed value becomes a clean validation diagnostic, not a YamlDotNet binding error; add `List<EvidenceCollector> EvidenceCollectors` to `GitOpsConfig`.
- [x] 1.2 In `src/Freeboard.Core/GitOps/ConfigModel.cs` add an optional `Evaluation` string field to the `Control` record (blank means absent).
- [x] 1.3 In `src/Freeboard.Core/GitOps/ConfigLoader.cs` add a `SchemaKeys` entry for `EvidenceCollector` (apiVersion, kind, id, title, control, vendor, type, frequency, threshold, config); add `evaluation` to the `Control` `SchemaKeys` set; add the `apiVersion` attribute override for `EvidenceCollector`; add the `switch (kind)` case that normalizes an explicit-null `config:` to an empty map (`collector with { Config = collector.Config ?? [] }`, mirroring the `MapsTo ?? []` control case, because a present-but-null YAML key overwrites the record default with null); add `EvidenceCollector` to the unknown-kind diagnostic message list.

## 2. Config: validation (feat(gitops): evidence-collector validation)

- [x] 2.1 In `src/Freeboard.Core/GitOps/ConfigValidator.cs` add `ValidateEvidenceCollectors`, called in phase order after controls, vendors, and requirements are validated (so their id sets exist). Require id/title/control/type/frequency; enforce unique id; resolve `control` against control ids and `vendor` (when present) against vendor ids; reject a `type` outside `integration`/`script`/`manual-attestation`/`training-attestation`/`agent`; reject a `frequency` outside the cadence set; parse the raw `threshold` text and reject a value that is present but is not an integer in `[0, 100]` (a non-integer produces a diagnostic, not a crash). Add case-sensitive `TryParse` helpers for the collector `type` and `frequency` sets (mirroring `TryParseDisposition`).
- [x] 2.2 In `ValidateControls` add the `evaluation` enum check: when a control's `evaluation` is present it must be one of `all`/`any`/`manual` (case-sensitive), else a diagnostic naming the control. Add a case-sensitive `TryParse` helper for the evaluation set.
- [x] 2.3 Add the conditional rule: in `ValidateEvidenceCollectors` collect the set of control ids that have at least one collector, then iterate the real `config.Controls` and, for each control that is in that set and whose `evaluation` is blank, emit a diagnostic naming the control and the missing evaluation rule. Drive the check off real controls (membership test), not off the collectors' control-refs, so a collector naming a non-existent control does not raise a spurious missing-evaluation diagnostic for an undefined id. A control with no collectors requires no rule.

## 3. Core tests (test(gitops): evidence-collector parse and validate)

- [x] 3.1 Add loader/validator unit tests: a valid `EvidenceCollector` and a `Control` with `evaluation` parse into the model; unknown field rejected; missing required fields rejected; unknown `type` rejected; unknown `frequency` rejected; a non-integer `threshold` (e.g. `high`) produces a validation diagnostic rather than a loader binding crash, and an out-of-range integer `threshold` is rejected; unknown `control`/`vendor` reference rejected; duplicate collector id rejected; wrong apiVersion rejected; optional `vendor`/`threshold`/`config` omitted still valid.
- [x] 3.2 Add tests for the evaluation rules: unknown `Control.evaluation` rejected; a control with a collector and no `evaluation` fails; a control with a collector and a valid `evaluation` passes; a control with no collector and no `evaluation` passes; a collector naming a non-existent control yields only the unknown-control diagnostic and no missing-evaluation diagnostic.
- [x] 3.3a Add malformed-`config` diagnostic tests: a `config` value that is not a scalar (a nested map or a sequence) makes the loader return a diagnostic, not throw an uncaught exception; assert the collector is reported as invalid and no other kind crashes the load. This pins the `string -> string` bind boundary (design "Nested config values" risk).
- [x] 3.3b Add an explicit-null `config` test: a collector authored with `config:` (the key present with no value, or an empty map) loads with `Config` normalized to an empty map (not null), passes validation, and round-trips through ImportPlan JSON serialization without throwing. This pins the loader `Config ?? []` normalization (design D1/D2).
- [x] 3.3 Add a valid multi-kind fixture including controls with `evaluation` and evidence-collectors (integration with vendor + config, and a manual-attestation without vendor) to the existing gitops test fixtures.
- [x] 3.4 Run `dotnet test tests/Freeboard.Core.Tests` - all green.

## 4. Persistence: schema and importer (feat(persistence): evidence-collector table and import)

- [x] 4.1 Add `src/Freeboard.Persistence/Migrations/012_evidence_collectors.sql`: `ALTER TABLE controls ADD COLUMN evaluation VARCHAR(16) NULL`; `CREATE TABLE IF NOT EXISTS evidence_collectors` per design D5 (utf8mb4_bin ids, DATETIME(6) timestamps, InnoDB; `id` PK, `api_version`, `title`, `control_id` NOT NULL FK -> controls RESTRICT, `vendor_id` NULL FK -> vendors RESTRICT, `type` VARCHAR(32), `frequency` VARCHAR(16), `threshold` INT NULL, `config` JSON NULL, timestamps; FK indexes on `control_id` and `vendor_id`; no secondary unique key).
- [x] 4.2 In `src/Freeboard.Persistence/GitOps/ImportPlan.cs` add `ControlRowPlan(Id, ApiVersion, Title, string? Evaluation)` and change `Controls` to `IReadOnlyList<ControlRowPlan>` (evaluation via `NullIfBlank`); add `EvidenceCollectorRowPlan(Id, ApiVersion, Title, Control, string? Vendor, Type, Frequency, int? Threshold, string? ConfigJson)` populated from the config (null vendor via blank; `int? Threshold` parsed from the raw `Threshold` text only after validation has passed - blank stays null; `ConfigJson` serialized from the config map with `System.Text.Json`, null when the map is empty), plus `EvidenceCollectorIds`. Keep `ControlRequirements` built from `config.Controls` as-is.
- [x] 4.3 In `src/Freeboard.Persistence/GitOps/MySqlGitOpsImporter.cs` add `UpsertControlsAsync` writing the `evaluation` column (controls no longer use the generic `UpsertAsync`); add `UpsertEvidenceCollectorsAsync` (insert/update by id) in the FK-safe upsert phase after controls and vendors; add `DeleteAbsentAsync("evidence_collectors", plan.EvidenceCollectorIds, ...)` in the prune phase BEFORE the absent-vendors, absent-controls, and absent-requirements deletes, so the collector RESTRICT FKs stay safe. Update the class summary comment to describe the new order.

## 5. Persistence: read store and counts (feat(persistence): evidence-collector read model)

- [x] 5.1 In `src/Freeboard.Persistence/ComplianceReadModels.cs` add `string? Evaluation` to `ControlRow`; add `EvidenceCollectorRow(Id, Title, Control, string? Vendor, Type, Frequency, int? Threshold, IReadOnlyDictionary<string, string> Config)`; extend `ComplianceCounts` with `EvidenceCollectors`.
- [x] 5.2 In `src/Freeboard.Persistence/MySqlComplianceStore.cs` select `evaluation` in `GetControlsAsync` and populate `ControlRow.Evaluation`; add `GetEvidenceCollectorsAsync` (read rows, deserialize `config` JSON to a `string -> string` map, empty when null); include the evidence-collector count in `GetCountsAsync`. Add `GetEvidenceCollectorsAsync` to `IComplianceStore.cs`.
- [x] 5.3 Add importer round-trip and read-store tests gated on `FREEBOARD_TEST_DB` (skip cleanly when unset): controls with an `evaluation` and evidence-collectors persist and read back, including `config`, `threshold` (null when absent), and `vendor` (null when absent). Adding an `evaluation` to an existing control preserves its other columns and cross-references.
- [x] 5.4 Add FK-safe resync prune-order integration tests (gated, mirror the vendor FK-safety resync tests in `tests/Freeboard.Persistence.Tests/MySqlIntegrationTests.cs`): a resync that removes a control a collector attaches to, and a resync that removes a vendor a collector names, each complete without hitting an `ON DELETE RESTRICT` violation because absent `evidence_collectors` are pruned before absent `controls`/`vendors`. Assert the stale collector is gone and the still-referenced rows that survive are correct. This pins the load-bearing delete order (design D5).
- [x] 5.5 Update every existing implementation and call site broken by the new members so the suite compiles: `tests/Freeboard.Web.Tests/FakeComplianceStore.cs` implements `GetEvidenceCollectorsAsync` (backed by a settable `Collectors` list, Guard-wrapped like the others) and adds `EvidenceCollectors` to its `ComplianceCounts`; every `new ComplianceCounts(...)` and `new ControlRow(...)` call site (at least `FakeComplianceStore.cs`, `ComplianceEndpointTests.cs`, `OrgSelectionTests.cs`, `MySqlIntegrationTests.cs`) is updated for the added members; CLI fakes in `tests/Freeboard.CLI.Tests/Fakes.cs` gain the new `IFreeboardApiClient` reads.

## 6. Web API read endpoints (feat(web): evidence-collector read endpoint)

- [x] 6.1 In `src/Freeboard/Compliance/ComplianceEndpoints.cs` add `evaluation` to the `/controls` payload (null when unset); add `GET /api/v1/freeboard/evidence-collectors` (GET-only, `RequireAuthorization`, NOT org-narrowed, wrapped in the existing `try/catch (Exception ex) when (IsStoreFailure(ex))` returning `Unreachable()` (503)) with payload `id`, `title`, `control`, `vendor`, `type`, `frequency`, `threshold`, `config`; add `evidenceCollectors` (camelCase) to the `/compliance/status` `persisted` object in BOTH the reachable (integer count) and unreachable (null) branches.
- [x] 6.2 Add endpoint tests (WebApplicationFactory + compliance store double): the collectors endpoint returns the seeded rows with `config`; `/controls` now includes `evaluation`; anonymous request returns 401; GET served in read-only mode; the collectors endpoint returns 503 (not an unhandled exception) when the store is unreachable; status reports the new integer count when reachable. Update the existing null-status assertion in `tests/Freeboard.Web.Tests/ComplianceEndpointTests.cs` to also assert `evidenceCollectors` is `JsonValueKind.Null`. Add a zero-org-access test (AuthWebFactory `AuthzMode = "Enforce"`, empty `FakeAuthzStore`) proving a zero-grant member still reads every collector.

## 7. Web SSR evidence-collector register page (feat(web): evidence-collector register page)

- [x] 7.1 Add `src/Freeboard/Pages/Compliance/EvidenceCollectors.cshtml` and `.cshtml.cs` (`@page "/compliance/evidence-collectors"`, PageModel injecting `IComplianceStore`, GET-only, store-unreachable notice) rendering each control with its `evaluation` rule and, under it, its attached collectors (type, vendor, frequency, threshold, config); a control with no collectors renders with a "no collectors" note. Model reads controls and collectors and groups collectors by control id (mirror `Vendors.cshtml.cs`).
- [x] 7.2 Add a nav link to the evidence-collector register in `src/Freeboard/Pages/Shared/_Layout.cshtml` alongside the existing compliance links.
- [x] 7.3 Add page tests: renders controls, their evaluation rule, and their collectors; anonymous GET redirects to `/login`; served in read-only mode; a zero-grant member under strict enforce still sees every control and collector (no narrowing). Add `/compliance/evidence-collectors` (with the required access level) to the parametrized `Pages` cases in `tests/Freeboard.WebE2E/AccessibilityAuditE2ETests.cs`.

## 8. CLI evidence-collector read command (feat(cli): collector list via API)

- [x] 8.1 In `src/Freeboard.CLI/IFreeboardApiClient.cs` add `ListControlsAsync` and `ListEvidenceCollectorsAsync` with wire records (`ApiControl` including `evaluation` and `maps_to`; `ApiEvidenceCollector` with id, title, control, vendor, type, frequency, threshold, config); implement both in `HttpFreeboardApiClient.cs` against `/controls` and `/evidence-collectors`.
- [x] 8.2 Add `src/Freeboard.CLI/CollectorCommands.cs` (`collector list`) modelled on `VendorCommands`, calling the shared `ApiCommandRunner.Run`/`Translate`, reading `/controls` and `/evidence-collectors` over HTTP and printing each control with its evaluation rule and, under it, its attached collectors including type, vendor, frequency, threshold, and each `config` key/value (same fields the SSR page renders); register the `collector` group in `Program.cs`.
- [x] 8.3 Add CLI tests using the `ApiClientFactory.Create` fake seam: `collector list` prints controls with their evaluation and collectors; assert the `config` key/value pairs of a seeded collector appear in the output; exit codes map correctly (0 ok, 1 validation, 3 operational). Put the new test class in the same xUnit collection as the existing user/vendor CLI tests so it does not run in parallel with them.

## 9. Sync summary parity (chore(cli): count evidence-collectors in gitops summaries)

- [x] 9.1 In `src/Freeboard.CLI/GitOpsCommands.cs` add the evidence-collector count to `PrintSummary`, `PrintPlannedState`, and the `Sync` success line.

## 10. Verification (chore: build and test)

- [x] 10.1 Run `dotnet build` - solution builds clean.
- [x] 10.2 Run `dotnet test` - unit/web/CLI tests pass; DB-gated tests skip cleanly without `FREEBOARD_TEST_DB`.
- [x] 10.3 If a MySQL is available, run the DB-gated tests with `FREEBOARD_TEST_DB` set and confirm the migration applies (table created, `controls.evaluation` added) and the round-trip passes.
- [x] 10.4 Run `npx markdownlint-cli2 "**/*.md"` for any touched Markdown and `openspec validate "add-evidence-collector-kind" --strict`.
